

        // for (let index = 0; index <= 5; index++) {
        //      console.log(`volta de numero: ${index}`)
        // }
        var times = ["vasco", "flamengo", "sp", "palmeiras"]
        var j = 0;
        for ( x in times) {
             console.log(`for in (${x})`)
              j++;
            // tras os index's
        }
        console.log(j)
        // for (x of times) {
        //     console.log(`for of (${x})`)
        //      //tras os valores 
        // }
        
// var voltas = 0;
// while (voltas <= 15) {
//     console.log(`volta de numero: ${voltas}`)
//     voltas++
// }

